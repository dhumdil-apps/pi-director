# Local guidance — <project>

Load `../AGENTS.md` before using this file; it is the portable contributor guide.

## Project memory

- Resolve and read `.agents/MEMORY.md` before broad exploration.
- Verify each entry against its named path, symbol, or command; code wins, and contradictions are corrected immediately.
- Keep only rediscovery-worthy Orientation/Quirks facts. Ordinary work may correct visible facts but never advances the hidden `memory-review` marker.

## Local integration

- Use `../run/dev/<slug>.sh` for the local environment and `../run/test/<slug>.sh` for the compact full suite when those launchers exist.
- (Add only facts this repo does not share with siblings.)
