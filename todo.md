# Maintainer notes

Session workflow lives in workspace `AGENTS.md`. This package is the Pi CLI
(status bar, usage, dashboard, progress widget). Context readout lives in
`extensions/progress-tracker/context-usage.ts`.
